/**
 *  主要功能入口，初始化菜单，页面标题等
 */
define(function (require, exports, module) {
    let main = {};
    main.mapEvent = require('./mapEvent');
    main.sceneEvent = require('./sceneEvent');

    main.addFW = function () {
      
    };
    module.exports = main;
});
